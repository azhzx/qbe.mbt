# clamp a value to [lo, hi]

export
function w $clamp(w %x, w %lo, w %hi) {
@start
	%c1 =w csltw %x, %lo
	jnz %c1, @lo, @c2
@c2
	%c2c =w csgtw %x, %hi
	jnz %c2c, @hi, @mid
@lo
	ret %lo
@hi
	ret %hi
@mid
	ret %x
}
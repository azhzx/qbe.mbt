# euclidean algorithm: gcd(1071, 462) = 21

export
function w $gcd(w %a, w %b) {
@start
	%r =w rem %a, %b
	%c =w ceqw %r, 0
	jnz %c, @done, @again
@again
	%r1 =w call $gcd(w %b, w %r)
	ret %r1
@done
	ret %b
}
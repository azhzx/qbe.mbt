# sum a run of doubles

export
function d $fsum(w %n) {
@start
	%s =d copy d_0.0
	%i =w copy 0
@loop
	%i2 =w phi @start %i, @body %i3
	%s2 =d phi @start %s, @body %s3
	%c =w csltw %i2, %n
	jnz %c, @body, @end
@body
	%f =d swtof %i2
	%s3 =d add %s2, %f
	%i3 =w add %i2, 1
	jmp @loop
@end
	ret %s2
}
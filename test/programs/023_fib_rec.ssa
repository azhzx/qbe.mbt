# recursive fibonacci: fib(10) = 55

export
function w $fib(w %n) {
@start
	%c =w csgtw %n, 1
	jnz %c, @rec, @base
@base
	ret %n
@rec
	%a =w sub %n, 1
	%b =w sub %n, 2
	%r1 =w call $fib(w %a)
	%r2 =w call $fib(w %b)
	%res =w add %r1, %r2
	ret %res
}